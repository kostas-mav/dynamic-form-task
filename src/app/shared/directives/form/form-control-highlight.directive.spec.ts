import { FormControlHighlightDirective } from './form-control-highlight.directive';

describe('FormControlHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new FormControlHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
