import { FormFooterDirective } from './form-footer.directive';

describe('FormFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new FormFooterDirective();
    expect(directive).toBeTruthy();
  });
});
